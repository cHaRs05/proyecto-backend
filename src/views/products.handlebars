<div class="max-w-7xl mx-auto p-6">
    <div class="flex justify-between items-center mb-10">
        <h1 class="text-4xl font-black text-gray-900">Catálogo de Productos</h1>
        <a href="/carts/694b5b5a2f56163bc935d758" class="bg-indigo-600 text-white px-6 py-3 rounded-xl font-bold hover:bg-indigo-700 transition shadow-lg">
            Ver mi Carrito 🛒
        </a>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {{#each products}}
        <div class="bg-white rounded-3xl shadow-xl p-8 border border-gray-100">
            <span class="bg-indigo-100 text-indigo-600 text-xs font-black px-3 py-1 rounded-full uppercase">{{this.category}}</span>
            <h2 class="text-2xl font-bold mt-4">{{this.title}}</h2>
            <p class="text-gray-500 text-sm my-4 italic">{{this.description}}</p>
            <div class="flex items-center justify-between">
                <p class="text-3xl font-black text-gray-900">${{this.price}}</p>
                <button 
                    onclick="agregarProducto('{{this._id}}')"
                    class="bg-gray-900 text-white p-4 rounded-2xl hover:bg-indigo-600 transition-all active:scale-95"
                >
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
                    </svg>
                </button>
            </div>
        </div>
        {{/each}}
    </div>
</div>

<script src="/js/cart.js"></script>