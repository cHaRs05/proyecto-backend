<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f0f4f8; }
        .product-card { transition: transform 0.2s; }
        .product-card:hover { transform: translateY(-3px); }
    </style>
</head>
<body class="p-4 md:p-10">
    <div class="max-w-4xl mx-auto">
        <h1 class="text-4xl font-bold text-gray-800 mb-8">
            Lista de Productos Estática
        </h1>
        <p class="text-gray-600 mb-6">Esta vista se carga una sola vez y no usa WebSockets. Visita 
            <a href="/realtimeproducts" class="text-indigo-600 font-semibold hover:text-indigo-800">/realtimeproducts</a> para la vista en tiempo real.
        </p>

        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
            {{#each products}}
                <div class="product-card bg-white p-5 rounded-lg shadow-lg border-t-4 border-indigo-500">
                    <h2 class="text-xl font-semibold text-gray-700 mb-2">{{this.title}}</h2>
                    <p class="text-3xl font-extrabold text-indigo-600 mb-3">${{this.price}}</p>
                    <p class="text-sm text-gray-500">Stock: {{this.stock}} | Código: {{this.code}}</p>
                    <p class="text-xs text-gray-400 mt-2">ID: {{this.id}}</p>
                </div>
            {{/each}}
            {{#unless products}}
                <p class="col-span-3 text-center text-gray-500 italic">El catálogo está vacío.</p>
            {{/unless}}
        </div>
    </div>
</body>
</html>